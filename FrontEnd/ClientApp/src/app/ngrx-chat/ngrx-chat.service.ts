import { Injectable } from '@angular/core';

// This will eventually get the store data from the backend.
// For now we are using NgrxChatServiceStub, which gets the data from ngx-chat-sample-data.ts.

@Injectable({
  providedIn: 'root'
})
export class NgrxChatService {

  constructor() { }
}
